# UI 风格重塑方案 v2

## 一、问题诊断

| 维度 | 现状 | 用户反馈 |
|---|---|---|
| 视觉调性 | 重度赛博朋克（glitch + scanline + 霓虹双色）| **"中二感太重"**，与"年轻人赛博卡片"的轻松定位错位 |
| 配色 | 全局固定 `#0a0a1a` 黑底 + 青紫双色 | **单调**，缺少个性表达 |
| 卡片 | 等距网格 + 角标装饰 + 发光边框 | **单调** |
| 结果页 | 长滚动页（标题 + 代号 + 8 卡片 + 总结 + CTA）| **太长**，分享截图需多张 |

## 二、设计转向：从「赛博朋克」→「赛博卡片」

> 关键词：**轻、个性、可分享**

### 1. 调性下调（保留赛博基因，去掉中二感）
- **保留**：等宽字体、代号体系（MBTI-脚本-荣格）、暗色基底
- **减弱**：glitch 双层抖动、扫描线密度、粗发光边
- **新增**：柔和的渐变光晕、更圆润的卡片圆角、轻量留白

### 2. MBTI 主题色系统（按荣格四分组）

| 群体 | 类型 | 主色 | HEX | 心理印象 |
|---|---|---|---|---|
| **分析师 NT** | INTJ/INTP/ENTJ/ENTP | 紫 | `#9b6bff` | 智识、神秘 |
| **外交官 NF** | INFJ/INFP/ENFJ/ENFP | 绿 | `#5fd9a8` | 共情、生长 |
| **守护者 SJ** | ISTJ/ISFJ/ESTJ/ESFJ | 蓝 | `#5fa8ff` | 稳定、可靠 |
| **探险家 SP** | ISTP/ISFP/ESTP/ESFP | 橙 | `#ff9b5f` | 活力、即兴 |

**注入方式**：
- 用户完成 MBTI 后，全局 `app.globalData.theme = '#9b6bff'`
- 各页面 `onShow` 读取主题色，注入 CSS 变量 `--theme`
- 卡片边框、按钮、进度条、glow 全部用 `--theme`，黑底保持不变（成为画布）
- **未做 MBTI 时**：保持中性银色 `#a0a8c0`，做完后整个 app"染色"

### 3. 结果页"一屏化"重设计

**目标**：竖屏 1 张截图（750×1334）覆盖全部信息

**新结构**（从上至下）：
```
┌─────────────────────────────────┐
│  CYBER ARCHIVE · 2026            │ ← 顶部细标
│  ╭─────────────────────────╮    │
│  │ INTJ-黑暗-异类           │    │ ← 主代号（巨字号）
│  │ 处女座·异类              │    │ ← 副标题昵称
│  ╰─────────────────────────╯    │
│                                 │
│  [紫] [紫] [紫] [紫]            │ ← 4 个核心维度（MBTI/星座/荣格/脚本）
│  [紫] [紫] [紫] [紫]            │ ← 4 个补充维度（依恋/压力/社交/HD）
│                                 │ ← 每格只显示「维度名 + label」，去掉 desc
│  ╭─────────────────────────╮    │
│  │ 你是异类思考者，处女座的    │    │ ← 1-2 行精炼总结（不再 typewriter）
│  │ 自律 + INTJ 的远见。       │    │
│  ╰─────────────────────────╯    │
│                                 │
│  cyber.archive / @yourname      │ ← 底部 watermark
└─────────────────────────────────┘
```

**长内容怎么办？**
- 详细解释挪到**底部"展开详情"**按钮 → 展开后变长页（默认折叠）
- 截图按钮永远基于"折叠态一屏"生成

### 4. 卡片重设计

**当前**：粗发光边 + 角标 + 大字号 label

**新版**：
- 卡片背景：`rgba(theme, 0.06)` 半透明色块
- 边框：`1rpx solid rgba(theme, 0.3)`，去掉发光动画
- 顶部 1 个图标 emoji（每个维度配一个：🧠 MBTI、♍ 星座、🎭 脚本、🌙 依恋…）
- 中间：维度名 + label
- 底部：去掉 desc，保留进度条（条数 = 候选数，亮的那条 = 你的结果）
- 圆角从 8rpx → 16rpx，更柔和

### 5. 全局风格降级清单

| 元素 | 现状 | 改为 |
|---|---|---|
| 全局扫描线 `.cyber-bg::before` | 4% 不透明度 | **移除** 或降到 1% |
| 全局扫描线动画 `.cyber-bg::after` | 移动光带 | **移除** |
| `.glitch` 双层抖动 | 持续 0.3s 抖 | **静态 + 微妙发光**，hover/tap 时才抖一下 |
| `.cyber-btn` | 双层边框 + hover 残影 | 单层 + 主题色填充态 + 按下淡化 |
| 字体 | 默认 sans-serif | 标题用 `JetBrains Mono`-like 等宽（已有），正文换回系统字体（更可读）|
| 配色饱和度 | 高饱和青/紫 | 主题色稍降饱和 + 加亮（更"年轻"）|

## 三、实施清单（按优先级）

### P0 — 必做（解决核心反馈）
- [ ] 1. 实现 MBTI 主题色系统（globalData + CSS 变量注入）
- [ ] 2. 结果页改"一屏式"，详情折叠
- [ ] 3. 移除全局扫描线 + 大幅减弱 glitch
- [ ] 4. 重做卡片视觉（emoji + 圆润边 + 进度条）

### P1 — 同步做（体验提升）
- [ ] 5. 截图按钮改为生成"折叠态一屏"，保证 1 张图覆盖
- [ ] 6. 每个维度配一个 emoji 图标
- [ ] 7. 卡片改用 16rpx 圆角 + 半透明色块

### P2 — 可选（锦上添花）
- [ ] 8. 首页根据已完成的 MBTI 染色背景
- [ ] 9. 结果页加 1 个"配色由 INTJ 决定"的彩蛋小字
- [ ] 10. 不同分组配不同字体重量

## 四、保留 vs 移除（一目了然）

**保留**：
- ✅ 暗色基底（仍是赛博底子）
- ✅ 等宽代号字体（CYBER ARCHIVE 那种）
- ✅ 8 维度档案的核心玩法
- ✅ 截图分享、长按重测、草稿恢复等功能

**移除/弱化**：
- ❌ 全屏扫描线动画
- ❌ 持续 glitch 抖动
- ❌ 粗发光边框 + 角标装饰
- ❌ 打字机效果（已经改成一次性显示）
- ❌ 长滚动结果页（改为一屏 + 折叠展开）

## 五、风险/取舍

1. **改完后"赛博感"会变弱** — 但换来"轻、个性、好分享"，符合用户原始定位
2. **主题色系统增加约 150 行代码** — 但只动 wxss 变量和 globalData，逻辑改动小
3. **结果页折叠后一些维度细节用户需要点击才看到** — 用动画过渡保证不突兀
