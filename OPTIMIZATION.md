# 赛博档案 · 优化方案

> 评估对象：当前 MVP 版本（5 页面 + 8 维度数据 + 本地 storage）
> 视角：产品体验 / 视觉表现 / 工程结构 / 性能 / 可维护性 / 增长

---

## 一、产品体验层面

### 1. 答题中断保护
**现状**：用户答到第 8 题退出小程序，下次回来要从第 1 题重答。  
**方案**：每答完一题就把 `{ dim, answers: [...] }` 存入 storage（key 如 `quiz_progress_attachment`），进入答题页时检测到草稿则弹窗"继续上次 / 重新开始"。  
**收益**：12 题对移动端用户偏长，断点续答能显著降低中途流失。

### 2. 答题反馈过弱
**现状**：点选项立即跳下一题，没有选中态、没有缓动。  
**方案**：
- 选中后给该选项 200ms 的高亮 + 边框流光动画，再切题
- 题目切换用左右滑动 / 淡入动画（`wx:if` + CSS transition）
- 进度条数字滚动

### 3. 结果页缺少"记忆点"
**现状**：8 格 + 一段文字，信息量大但没有焦点。  
**方案**：
- 顶部加一个"总标签"：例如根据 MBTI + 人生剧本 + 荣格三个核心维度生成一个**复合代号**（如 `INTJ-导演-智者 · 暗夜观测者`），增强用户分享冲动
- 每个格子加一个细分**百分比 / 分布雷达图**（用 canvas 或纯 wxss 画）—— 让用户看到自己在四个候选里的"倾向比例"，比单一标签更可信
- 总结文字逐字打字机效果，赛博感更强

### 4. 分享体验
**现状**：只支持 `onShareAppMessage`，缺自定义封面图。  
**方案**：
- 提供静态分享封面（设计图，标语 + 主视觉）放在 `images/share-cover.png`，`onShareAppMessage` 返回 `imageUrl`
- 使用 `wx.canvasToTempFilePath` 把结果卡片生成图片，提供"保存到相册"按钮（朋友圈传播主路径）
- 增加 `onShareTimeline`（已添加，但需要在 app.json 配置或在页面 json 里加）

### 5. 用户没有"再来一次单项"动机
**现状**：重测只能全部清空。  
**方案**：在主页已完成的格子上长按或右上角加一个小"↻"图标，单维度重测。

### 6. 缺少进入引导
**现状**：第一次进入直接看到 8 个空格子，新用户可能不理解。  
**方案**：首次进入时显示一个 mask 引导："这 8 个维度会从不同角度扫描你 → 全部完成生成完整档案 → 数据只保存在你的手机上"。

---

## 二、视觉与赛博朋克风格强化

当前样式做到了"配色对"，但赛博朋克的**纹理层**还薄。

| 现状 | 优化方向 |
|---|---|
| 静态边框 + 阴影 | 边框加流光动画（`@keyframes` 让 box-shadow / border-color 循环变化） |
| 纯背景渐变 | 叠加细网格纹（CSS repeating-linear-gradient 模拟扫描线） |
| 标题文字 | 双层错位文字（一份青色一份紫色，做 glitch chromatic aberration 效果） |
| 卡片填充 | 完成的格子加"扫描进度条"完成动画首次出现时 |
| 字体 | 引入数字感字体（如等宽 monospace）做编号 / 进度数字 |

不建议引入图片资源（包体积），用纯 CSS 即可。

---

## 三、工程结构

### 1. 数据层
**现状**：`dimensions.js` + `quizzes.js` 分两个文件，但维度与题库的耦合关系散落两处。  
**方案**：合并成 `data/<dim>.js`，每个维度一个文件，导出 `{ meta, results, questions }`。`utils/dimensions.js` 只做汇总 index。好处：增删维度只动一个文件，未来想"动态加载某些维度"也容易。

### 2. 抽象计票逻辑
**现状**：`calcQuizResult` 简单计数，"先达到该次数"作为平局规则——会让用户看不到的"题目顺序"影响结果。  
**方案**：
- 平局时回退到题目的**权重设计**（关键题加权）
- 或者明确文档化平局规则（按 type 字典顺序优先）
- 极端情况（如 4 类各 3 票）给用户看一个"你处在 X 和 Y 之间"的双标签

### 3. 路由参数与常量
**现状**：`'/pages/quiz/index?dim=attachment'` 字符串硬编码在 dimensions.js。  
**方案**：在 dimensions 里只放 `key`，在 index 页面 onCellTap 里根据 `type` 拼 url，避免重复维护路径。

### 4. Storage Schema 版本化
**现状**：直接读写 `cyber_archive_results`。  
**方案**：写一层薄封装 `{ version: 1, data: {...} }`，未来题目改版后能通过 version 触发"你的旧档案已过期"流程，避免脏数据。

### 5. 清理无用代码
- `cloudfunctions/` 整个目录可以删（已不使用云开发）
- `miniprogram/pages/example/` 残留模板页应删
- `project.config.json` 的 `cloudfunctionRoot` 字段、`condition.miniprogram.list` 里的 db guide 引用可清理
- `envList.js`、`uploadCloudFunction.sh` 都可删

### 6. TypeScript 化（可选）
维度类型 / 结果 type / 选项 type 用 ts 能避免拼写错误。微信开发者工具已原生支持 TS。投入产出比中等，看后续是否长期维护。

---

## 四、性能与体积

1. **`quizzes.js` 约 30KB**：完全在小程序首启时全量 require。可改为 `pages/quiz/index.js` 进入时按需 `require('../../data/' + dim + '.js')` —— 微信小程序支持动态字符串 require（运行时存在），但官方限制需要静态可分析，因此更稳妥做法是分包：

```
subpackages: [
  { root: "quiz-pkg", pages: ["pages/quiz/index"] }
]
```
答题页放分包，首页打开更快。

2. **图片资源**：当前 `images/icons/` 空，未来加 logo / 分享图时全部走分包或网络图。

3. **app.wxss 体积**：当前每个页面 wxss 都重复定义渐变背景，可提到 app.wxss 统一作为 `.cyber-bg` 类。

---

## 五、可访问性与边界

- **答题进度时返回主页**：当前 quiz 页中途返回不会保存进度，用户答到一半切到主页就清空。需要明确产品决策（保存 or 不保存）。
- **storage 失败兜底**：当前 storage.js try/catch 了但页面没处理 set 失败。极少出现但 iOS 隐私模式可能。
- **键盘 / 屏幕适配**：长题目 + 长选项在小屏机（iPhone SE）上会挤，需要测试 320px 宽度。
- **深色模式 / 系统主题**：当前固定深色，与系统主题切换无冲突，但 status bar 颜色（`navigationBarBackgroundColor`）已经设置 OK。

---

## 六、增长 / 数据

虽然 PRD 写"纯前端不需要服务器"，但建议预留埋点点位：

- 进入主页 / 进入每个维度 / 答题完成 / 生成档案 / 触发分享
- 用一个空函数 `track(event, payload)` 封装，未来想接微信小程序数据助手 / 自建后端时只改一处
- 完整档案完成率、各维度完成率是产品迭代的核心指标

---

## 七、优先级建议

| 优先级 | 项目 | 理由 |
|---|---|---|
| P0 | 清理云开发残留 + 删 example 页 | 1 分钟可做，立刻清爽 |
| P0 | 答题断点续答 | 直接影响完成率 |
| P0 | 分享自定义封面 + 保存结果图 | 这是增长唯一抓手 |
| P1 | 结果页复合代号 + 打字机动画 | 提升记忆点 |
| P1 | 答题选项选中动画 | 体验细节 |
| P1 | 视觉强化（扫描线 / 流光 / glitch） | 与"赛博"主题强相关 |
| P2 | 数据层重构（按维度拆文件） | 长期可维护性 |
| P2 | 分包加载 | 体积/速度优化 |
| P2 | 埋点桩 | 为后续数据决策铺路 |
| P3 | TypeScript 化 | 投入大、收益靠长期 |

---

## 八、还需要确认的产品决策

1. 答题中途退出，进度是否保存？
2. 单维度是否允许"重测"？
3. 结果页是否需要"生成图片保存到相册"按钮？
4. 是否需要复合代号 / 雷达图等增强结果页？
5. 是否要把云开发相关代码彻底清掉？
